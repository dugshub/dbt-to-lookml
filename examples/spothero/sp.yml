project: spothero

input: .
output: ./lookml
schema: gold

model:
  name: spothero
  connection: database

explores:
  - fact: rentals
  - fact: facility_lifecycle

output_options:
  clean: clean
  manifest: true

options:
  dialect: redshift
  pop_strategy: dynamic
  date_selector: true
  convert_tz: false
