metrics:
  - name: user_to_session_ratio
    type: ratio
    type_params:
      numerator: user_count
      denominator: session_count
    label: User to Session Ratio
    description: Ratio of users to sessions (should fail - no FK path)
    meta:
      primary_entity: user
