metrics:
  - name: total_revenue_metric
    type: simple
    type_params:
      measure: total_revenue
    label: Total Revenue
    description: Sum of all revenue
    meta:
      primary_entity: rental_sk

  - name: user_count_metric
    type: simple
    type_params:
      measure: user_count
    label: Total Users
    meta:
      primary_entity: user_sk
